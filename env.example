# Environment Variables Configuration
# Copy this file to .env.local for local development
# Set these variables in Vercel dashboard for production

# ==========================================
# DATABASE CONFIGURATION
# ==========================================
# Use separate databases for dev and production
# Local/Dev example:
DATABASE_URL="postgresql://username:password@localhost:5432/gifts_dev?schema=public"
# Production: Set this in Vercel dashboard to your production database

# ==========================================
# ACCESS TOKENS
# ==========================================
# Generate separate tokens for dev and production environments
# IMPORTANT: Use different tokens for each environment!

# User Access Token (for gift request authentication)
# Dev token example (CHANGE THIS!):
USER_ACCESS_TOKEN="gift_access_dev_CHANGE_THIS_TO_RANDOM_32_CHAR_STRING"

# Admin Access Token (for admin panel authentication)
# Dev token example (CHANGE THIS!):
ADMIN_ACCESS_TOKEN="admin_access_dev_CHANGE_THIS_TO_RANDOM_32_CHAR_STRING"

# ==========================================
# NODE ENVIRONMENT
# ==========================================
NODE_ENV="development"

# ==========================================
# INSTRUCTIONS
# ==========================================
# 1. Copy this file to .env.local for local development
# 2. Generate secure random tokens using:
#    openssl rand -hex 32
# 3. Never commit .env.local or .env to version control
# 4. Set production tokens in Vercel dashboard (different from dev!)
# 5. Production tokens should NEVER be in local/dev .env files

# ==========================================
# GENERATING SECURE TOKENS
# ==========================================
# Run these commands to generate secure tokens:
# 
# For User Access Token:
#   echo "USER_ACCESS_TOKEN=\"gift_access_$(openssl rand -hex 32)\""
#
# For Admin Access Token:
#   echo "ADMIN_ACCESS_TOKEN=\"admin_access_$(openssl rand -hex 32)\""

